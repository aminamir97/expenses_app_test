import 'package:flutter/material.dart';

class AppConstants {
  static const designSize = Size(414, 896);
  static const title = "Expenses App";
  static const appIdentifier = "com.expense.app";

  static const cacheHeight = 500;
  static const splashDuration = Duration(seconds: 3);
  static const onboardingDuration = Duration(milliseconds: 500);
}
