import 'package:flutter/rendering.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppPaddings {
  static const zero = EdgeInsets.zero;

  static final normal = EdgeInsets.all(30.sp);
  static final normalX = EdgeInsets.symmetric(horizontal: 20.w);
  static final normalY = EdgeInsets.symmetric(vertical: 20.h);

  static final small = EdgeInsets.all(12.sp);
  static final smallY = EdgeInsets.symmetric(vertical: 8.h);
  static final smallX = EdgeInsets.symmetric(horizontal: 8.w);
}
